#include <stdio.h>
#include <stdlib.h>

#include "stack.h"

#define MAX 15







void main()
{
	int a[MAX][MAX];
	int exit = MAX / 2;
	int i, j;
	lElem *head;

	head = NULL;

	//printf("LOL\n");

	/*

	for (i=0; i<MAX; i++)
		for(j=0; j<MAX; j++)
			a[i][j] = 0;

	a[exit][29] = 1;

	for (i=0; i<MAX; i++, printf("\n"))
		for(j=0; j<MAX; j++)
			printf("%d", a[i][j]);


	*/




	//additional edit
}